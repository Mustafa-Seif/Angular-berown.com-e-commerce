import { SliceNamePipe } from './slice-name.pipe';

describe('SliceNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SliceNamePipe();
    expect(pipe).toBeTruthy();
  });
});
